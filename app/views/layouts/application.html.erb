<!DOCTYPE html>
<html>
    <head>
        <title>CamClickr</title>
        <%= csrf_meta_tags %>
        <%= csp_meta_tag %>
        
        <%= stylesheet_link_tag    'application', media: 'all' %>
        <%= javascript_include_tag 'application' %>

        <%= favicon_link_tag asset_path('upload-bar/favicon.png') %>

        <meta property="og:title" content="Cam Clickr" />
        <meta property='og:image' content="<%= image_url('welcome_page.png') %>" />
        <meta property="og:description" content="Cam Clickr is a Flickr clone designed to replicate its photo sharing and organizing capabilities." />
        <meta property="og:url" content="https://cam-clickr.herokuapp.com/#/" />

        <script>
        window.logo = "<%= image_url("upload-bar/flickr-logo.png") %>"
        window.uploadButton = "<%= image_url("upload-bar/upload-icon.png") %>"

        window.onclick = event => {
            if (!event.target.matches('.dropdown-button')) {
                var dropdowns = document.getElementsByClassName("dropdown");
                var arrows = document.getElementsByClassName("arrow-up");
                var i;
                for (i = 0; i < dropdowns.length; i++) {
                    var openDropdown = dropdowns[i];
                    var arrow = arrows[i];
                    if (openDropdown.classList.contains('show')) {
                        openDropdown.classList.remove('show');
                    }
                    if (arrow.classList.contains('show')) {
                        arrow.classList.remove('show');
                    }
                }
            }
        }
        </script>
    </head>

    <body>
        <%= yield %>
    </body>
</html>
